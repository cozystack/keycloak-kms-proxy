// Package keycloakmodel holds the parsed Keycloak schema and query model:
// tables, PII columns, named queries, and the
// parameter→column bindings used to generate the conformance test corpus.
package keycloakmodel
